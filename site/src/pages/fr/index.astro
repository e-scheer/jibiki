---
import LandingPage from '../../components/LandingPage.astro';
import { publicConfig } from '../../config';
import { getMessages } from '../../i18n/messages';
import BaseLayout from '../../layouts/BaseLayout.astro';

const t = getMessages('fr');
const schema = [
  {
    '@context': 'https://schema.org',
    '@type': 'Organization',
    name: 'jibiki',
    url: publicConfig.siteUrl,
    logo: `${publicConfig.siteUrl}/brand/jibiki-mark.svg`,
  },
  {
    '@context': 'https://schema.org',
    '@type': 'WebSite',
    name: 'jibiki',
    url: `${publicConfig.siteUrl}${t.routes.home}`,
    description: t.meta.homeDescription,
    inLanguage: 'fr',
  },
  {
    '@context': 'https://schema.org',
    '@type': 'SoftwareApplication',
    name: 'jibiki',
    applicationCategory: 'EducationalApplication',
    url: publicConfig.appUrl,
    description: t.meta.homeDescription,
    inLanguage: ['fr', 'en'],
  },
];
---

<BaseLayout locale="fr" t={t} title={t.meta.homeTitle} description={t.meta.homeDescription} path={t.routes.home} alternatePath="/en/" alternateLocale="en" schema={schema}>
  <LandingPage locale="fr" t={t} />
</BaseLayout>
